import { StackDirective } from './stack.directive';

describe('StackDirective', () => {
  it('should create an instance', () => {
    const directive = new StackDirective();
    expect(directive).toBeTruthy();
  });
});
