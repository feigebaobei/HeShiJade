// todo 可能要删除
import type { Options, S,
    //  A, ULID, B, N, 
    // Options, 
    B,
    N,
    ConfigItem,
    } from "./base"
type Type = 'select' | 'input' | 'textarea'

export {
    // BehaviorItem,
    // BehaviorMeta,
    // BehaviorItemKey,
    // BehaviorConfigItem,
}