import { ItemCategoryDirective } from './item-category.directive';

describe('ItemCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
