import { ItemsDirective } from './items.directive';

describe('ItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemsDirective();
    expect(directive).toBeTruthy();
  });
});
